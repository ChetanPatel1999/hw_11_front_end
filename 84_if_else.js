//wap to check given alpha is vovel or consonenet.
let ch;
ch = prompt("enter a alpha : ");
if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') {
    document.write("alpha is vovel");
}
else {
    document.write("alpha is consonent");
}